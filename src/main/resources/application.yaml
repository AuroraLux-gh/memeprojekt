spring:
  servlet:
    multipart:
      max-file-size: "200MB"
      max-request-size: "200MB"
  jpa:
    properties:
      hibernate:
        show_sql: false
        use_sql_comments: true
    open-in-view: true
    hibernate:
      ddl-auto: "update"
    database-platform: "org.hibernate.dialect.MariaDBDialect"
  datasource:
    driver-class-name: "com.mysql.cj.jdbc.Driver"
    username: "root"
    password: "mauer137"
    url: "jdbc:mysql://127.0.0.1:3306/memedb"